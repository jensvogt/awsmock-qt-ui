name: Qt CMake Build (Windows)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build_windows:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup MSVC Environment
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Python + aqtinstall
        run: |
          python -m pip install --upgrade pip
          pip install aqtinstall

      # -------------------------------
      # Install Qt (base + modules)
      # -------------------------------
      - name: Install Qt 6.10.1 + modules
        run: |
          python -m aqt install-qt windows desktop 6.10.1 win64_msvc2022_64 --outputdir Qt --modules qtcharts qtimageformats

      - name: Show Qt directory structure
        run: |
          echo "Qt version directory:"
          dir Qt\6.10.1
          echo "Qt platform directory:"
          dir Qt\6.10.1\msvc2022_64
          echo "CMake files:"
          dir Qt\6.10.1\msvc2022_64\lib\cmake\Qt6

      # -------------------------------
      # Configure & Build
      # -------------------------------
      - name: Configure and Build Application
        shell: powershell
        run: |
          cmake -B build -S . `
            -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_PREFIX_PATH="${PWD}\Qt\6.10.1\msvc2022_64"

          cmake --build build --config Release

      # -------------------------------
      # Archive Executable
      # -------------------------------
      - name: Archive Executable
        shell: powershell
        run: |
          Copy-Item build\your-app-name.exe your-app-name-windows-x64.exe

      # -------------------------------
      # Optional Release Publishing
      # -------------------------------
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: your-app-name-windows-x64.exe
          name: Release ${{ github.ref_name }}
          draft: true

      # -------------------------------
      # Upload Artifact
      # -------------------------------
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: qt-app-executable-windows
          path: your-app-name-windows-x64.exe
          retention-days: 7
